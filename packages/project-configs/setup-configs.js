/* eslint-disable */
const fs = require('fs');
const path = require('path');

const pathsTo = {
  projectRoot: process.cwd(),
  configs: __dirname,
};
// todo: auto setup all configs in root with extends

/* eslint-enable */
